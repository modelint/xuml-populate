// Explicit types and tuple scalar output
i::Integer = ival
x,y = .compute()
// Create and relate
*[ A() &R3 a, b; B() ]
&R1 that one
*Taxiing Aircraft(ID: in.id, Heading: in.initial heading) &R1 assigned runway, trun
*A()
=>> result